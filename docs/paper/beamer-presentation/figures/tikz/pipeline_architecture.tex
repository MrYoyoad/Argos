% Pipeline architecture diagram — 8-stage modular pipeline
% Two-row layout: pre-processing (top) → inference + output (bottom)
% Colors: ArgosTeal=pre-processing, ArgosOrange=AI inference, ArgosGreen=output
\begin{tikzpicture}[
  % ── Box styles ──────────────────────────────────────────────────────
  pre/.style={
    draw=ArgosTeal, fill=ArgosTeal!10, line width=1.2pt,
    rounded corners=6pt,
    minimum height=1.15cm, minimum width=2.4cm,
    align=center, inner sep=6pt, text=ArgosNavy
  },
  infer/.style={
    draw=ArgosOrange, fill=ArgosOrange!10, line width=1.2pt,
    rounded corners=6pt,
    minimum height=1.15cm, minimum width=2.4cm,
    align=center, inner sep=6pt, text=ArgosNavy
  },
  outstage/.style={
    draw=ArgosGreen, fill=ArgosGreen!10, line width=1.2pt,
    rounded corners=6pt,
    minimum height=1.15cm, minimum width=2.4cm,
    align=center, inner sep=6pt, text=ArgosNavy
  },
  % ── Number badge ─────────────────────────────────────────────────────
  num/.style={
    circle, minimum size=16pt, inner sep=0pt,
    font=\scriptsize\bfseries, text=white, fill=#1
  },
  % ── Arrows ───────────────────────────────────────────────────────────
  fwd/.style={-{Stealth[length=3mm,width=2mm]}, line width=1pt, ArgosGray},
  corner/.style={-{Stealth[length=3mm,width=2mm]}, line width=1pt, ArgosGray,
                 rounded corners=6pt},
  % ── Phase label ──────────────────────────────────────────────────────
  phase/.style={font=\tiny\bfseries, #1, anchor=west},
  node distance=0.55cm and 0.45cm
]

% ── Phase band backgrounds ────────────────────────────────────────────
% (drawn first so boxes sit on top)
\begin{scope}
  \fill[ArgosTeal!5, rounded corners=6pt]
    (-0.15,-0.05) rectangle (10.55, 1.60);
  \fill[ArgosOrange!5, rounded corners=6pt]
    (-0.15,-2.90) rectangle (5.35,-1.35);
  \fill[ArgosGreen!5, rounded corners=6pt]
    (5.55,-2.90) rectangle (10.55,-1.35);
\end{scope}

% ══════════════════════════════════════════════════════════════════════
% ROW 1 — Pre-processing
% ══════════════════════════════════════════════════════════════════════

\node[pre] (s1) at (0.6, 0.75)
  {{\small\bfseries Normalize}\\[-1pt]{\tiny GPU/HDR conversion}};

\node[pre] (s2) at (3.15, 0.75)
  {{\small\bfseries ASR}\\[-1pt]{\tiny Whisper transcript}};

\node[pre] (s3) at (5.7, 0.75)
  {{\small\bfseries LRS3 Prep}\\[-1pt]{\tiny Format conversion}};

\node[pre] (s4) at (8.25, 0.75)
  {{\small\bfseries Manifests}\\[-1pt]{\tiny TSV + splits}};

% Phase label (top row)
\node[phase=ArgosTeal, above=2pt of s1, xshift=-2pt]
  {\textcolor{ArgosTeal}{PRE-PROCESSING}};

% ══════════════════════════════════════════════════════════════════════
% ROW 2 — Inference + Output
% ══════════════════════════════════════════════════════════════════════

\node[infer] (s5) at (0.6, -2.15)
  {{\small\bfseries Clustering}\\[-1pt]{\tiny K-means features}};

\node[infer] (s6) at (3.15, -2.15)
  {{\small\bfseries Decode}\\[-1pt]{\tiny VSP-LLM inference}};

\node[outstage] (s7) at (5.7, -2.15)
  {{\small\bfseries Reports}\\[-1pt]{\tiny JSON + metrics}};

\node[outstage] (s8) at (8.25, -2.15)
  {{\small\bfseries Burns}\\[-1pt]{\tiny Subtitled videos}};

% Phase labels (bottom row)
\node[phase=ArgosOrange, above=2pt of s5, xshift=-2pt]
  {\textcolor{ArgosOrange}{AI INFERENCE}};
\node[phase=ArgosGreen, above=2pt of s7, xshift=-2pt]
  {\textcolor{ArgosGreen}{OUTPUT}};

% ══════════════════════════════════════════════════════════════════════
% Stage number badges
% ══════════════════════════════════════════════════════════════════════

\node[num=ArgosTeal]  at ([xshift=-11pt,yshift=11pt]s1.north west) {1};
\node[num=ArgosTeal]  at ([xshift=-11pt,yshift=11pt]s2.north west) {2};
\node[num=ArgosTeal]  at ([xshift=-11pt,yshift=11pt]s3.north west) {3};
\node[num=ArgosTeal]  at ([xshift=-11pt,yshift=11pt]s4.north west) {4};
\node[num=ArgosOrange] at ([xshift=-11pt,yshift=11pt]s5.north west) {5};
\node[num=ArgosOrange] at ([xshift=-11pt,yshift=11pt]s6.north west) {6};
\node[num=ArgosGreen]  at ([xshift=-11pt,yshift=11pt]s7.north west) {7};
\node[num=ArgosGreen]  at ([xshift=-11pt,yshift=11pt]s8.north west) {8};

% ══════════════════════════════════════════════════════════════════════
% Arrows — Row 1
% ══════════════════════════════════════════════════════════════════════

\draw[fwd] (s1.east) -- (s2.west);
\draw[fwd] (s2.east) -- (s3.west);
\draw[fwd] (s3.east) -- (s4.west);

% ══════════════════════════════════════════════════════════════════════
% Corner connector: end of Row 1 → start of Row 2
% ══════════════════════════════════════════════════════════════════════

\draw[corner] (s4.east)
  -- ++(0.35, 0)
  -- ++(0, -2.90)
  -- (s5.west |- s5.center)
  -- (s5.west);

% ══════════════════════════════════════════════════════════════════════
% Arrows — Row 2
% ══════════════════════════════════════════════════════════════════════

\draw[fwd] (s5.east) -- (s6.west);
\draw[fwd] (s6.east) -- (s7.west);
\draw[fwd] (s7.east) -- (s8.west);

% ══════════════════════════════════════════════════════════════════════
% Input / Output labels
% ══════════════════════════════════════════════════════════════════════

\node[font=\tiny\bfseries, ArgosGray, anchor=east, align=right] at ([xshift=-6pt]s1.west)
  {Video\\Input};
\node[font=\tiny\bfseries, ArgosGray, anchor=west, align=left] at ([xshift=6pt]s8.east)
  {Reports\\+ Videos};

\draw[fwd, ArgosGray!60] ([xshift=-14pt]s1.west) -- (s1.west);
\draw[fwd, ArgosGray!60] (s8.east) -- ([xshift=14pt]s8.east);

\end{tikzpicture}