# Supplementary Reports — Frozen Snapshot

Frozen snapshot of Reports 1–6 plus 3 analysis docs at deck delivery (May 2026). Canonical living docs are at `docs/<topic>/report_N_*.md` (e.g. `docs/evaluation/`, `docs/tuning/`, `docs/confidence/`, `docs/prompts/`, `docs/finetuning/`, `docs/beam-search/`). Numbers in this snapshot may differ from the live `docs/` tree as the latter is kept current with `after_amosi_audit.md`.
